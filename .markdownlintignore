# VENDORED spec-kit text. `specify init --force` rewrites every one of these from
# upstream, so a lint fix here is destroyed on the next re-vendor — which is the
# whole job of scripts/fleet-speckit-upgrade.sh. Upstream's to fix, not ours.
#
# DECLARED, not inherited, for the reason internal-claude-workflows records:
# `markdownlint .` does not descend into dot-directories, so without this file
# `.claude/**` and `.specify/**` are not linted at all and a "clean repo" covers
# less than it claims. An accidental exclusion and a deliberate one produce the
# same clean output. Measured 2026-08-31: the nine speckit-* skills below carry
# 138 findings and `.specify/` carries 7, none of them reachable by an edit that
# survives the next upgrade wave.
.claude/skills/speckit-analyze/
.claude/skills/speckit-checklist/
.claude/skills/speckit-clarify/
.claude/skills/speckit-constitution/
.claude/skills/speckit-converge/
.claude/skills/speckit-implement/
.claude/skills/speckit-plan/
.claude/skills/speckit-specify/
.claude/skills/speckit-tasks/
.claude/skills/speckit-taskstoissues/
.specify/

# VENDORED document skills (Anthropic's docx/pptx/xlsx bundle). Same rule: this
# repo carries them as-is so `newproject` seeds them, and editing them in place
# makes the next bundle refresh a merge conflict instead of a copy.
.claude/skills/docx/
.claude/skills/pptx/
.claude/skills/xlsx/
